# Rename Malcolm to httpSandwich

## Summary

Rename all occurrences of "Malcolm" to "httpSandwich" throughout the codebase. The canonical format is `httpSandwich` (lowercase "http" + capitalized "Sandwich"). For npm package name, use `httpsandwich` (npm requires lowercase).

## Files to Update

### Core Configuration

**[package.json](package.json)**

- Line 2: `"name": "malcolm"` → `"name": "httpsandwich"`
- Line 4: `"description": "Malcolm - HTTP..."` → `"description": "httpSandwich - HTTP..."`
- Line 10: `"bin": { "malcolm": ... }` → `"bin": { "httpSandwich": ... }`

**[package-lock.json](package-lock.json)** - Will regenerate via `npm install` after package.json changes

### Source Code

**[src/cli/main.ts](src/cli/main.ts)**

- Line 2: Comment `Malcolm - HTTP monitor-in-the-middle` → `httpSandwich - HTTP monitor-in-the-middle`
- Line 67: `"Malcolm stopped."` → `"httpSandwich stopped."`

**[src/cli/argument-parser.ts](src/cli/argument-parser.ts)**

- Line 24: Comment format `malcolm --from ...`
- Lines 114, 136-138: Usage examples `malcolm --from ...` → `httpSandwich --from ...`

**[src/application/services/tui-layout.ts](src/application/services/tui-layout.ts)**

- Line 66: TUI header `Malcolm` → `httpSandwich`

**[src/infrastructure/file-exchange-store.ts](src/infrastructure/file-exchange-store.ts)**

- Line 9: Comment about storage path
- Line 17: `tmpdir(), "malcolm"` → `tmpdir(), "httpSandwich"`

### Test Files

**[tests/unit/application/tui-layout.test.ts](tests/unit/application/tui-layout.test.ts)**

- Line 71: `expect(headerContent).toContain("Malcolm")` → `toContain("httpSandwich")`
- Line 82: Path `/tmp/malcolm-exchanges` → `/tmp/httpSandwich-exchanges`
- Line 101: Path `/malcolm/exchanges/` → `/httpSandwich/exchanges/`

**[tests/unit/application/screen-renderer.test.ts](tests/unit/application/screen-renderer.test.ts)**

- Line 128: `expect(allText).toContain("Malcolm")` → `toContain("httpSandwich")`

### Documentation

**[docs/plans/2025-12-06-1-project-setup.md](docs/plans/2025-12-06-1-project-setup.md)**

- Line 1: Title and references

**[docs/plans/2025-12-06-2-mvp.md](docs/plans/2025-12-06-2-mvp.md)**

- CLI examples and references

**[docs/plans/2025-12-06-3-levels.md](docs/plans/2025-12-06-3-levels.md)**

- Title and storage path references

**[docs/plans/2025-12-06-4-tui.md](docs/plans/2025-12-06-4-tui.md)**

- Title and TUI layout examples

### Cursor Rules

**[.cursor/rules/code-architecture-and-design.mdc](.cursor/rules/code-architecture-and-design.mdc)**

- Line 2: Description
- Line 10: Content reference

**[.cursor/rules/testing-and-quality.mdc](.cursor/rules/testing-and-quality.mdc)**

- Line 2: Description
- Line 11: Content reference

### IDE Configuration (IntelliJ IDEA)

**[.idea/malcolm.iml](.idea/malcolm.iml)**

- Rename file to `httpSandwich.iml`

**[.idea/modules.xml](.idea/modules.xml)**

- Line 5: Update module reference from `malcolm.iml` to `httpSandwich.iml`

## Verification Steps

1. Run `find . -iname "*malcolm*" -not -path "*/node_modules/*" -not -path "*/.git/*"` to confirm no files/folders contain "malcolm"
2. Run `grep -ri "malcolm" --include="*.ts" --include="*.json" --include="*.md" --include="*.mdc"` to confirm no content references remain
3. Run `nvm use` to ensure correct Node.js version
4. Run `npm install` to regenerate package-lock.json
5. Run `npm run build` to verify TypeScript compilation
6. Run `npm run quality` to verify all checks pass (linting, formatting, type-check, tests)
7. Run `npm start -- --help` to verify CLI usage output shows correct name

## Notes

- The `dist/` folder will be regenerated by `npm run build` and doesn't need manual updates
- npm package names must be lowercase, so the package name will be `httpsandwich`
- The executable/binary name will be `httpSandwich` (preserving the canonical casing)
